// models/Link.js (For reference - In a real app, you'd use a real database)
/**
 * Link model structure:
 * {
 *   id: string,
 *   url: string,
 *   title: string,
 *   summary: string,
 *   keywords: string[],
 *   tags: string[],
 *   status: string ('unread' | 'read' | 'favorite'),
 *   createdAt: string (ISO date)
 * }
 */
